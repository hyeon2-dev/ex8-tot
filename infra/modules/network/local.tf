locals {
    owner       = var.owner
    vpc_cidr    = var.vpc_cidr
    tag_header  = var.tag_header
    subnet_map  = var.subnet_map
    region      = var.region
    az_names    = var.az_names
}